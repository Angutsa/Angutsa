# Program to determine how long savings will take to reach a down payment


# Ask user for annual salary, percentage of salary to save and the cost of the house
annual_salary = int(input("Enter your annual salary: "))
portion_saved = float(input("Enter the percent of your salary to save, as a decimal: ")) # percentage of annual salary saved
total_cost = int(input("Enter the cost of your dream home: "))

# Determine the down payment
portion_down_payment = 0.25
down_payment = portion_down_payment * total_cost

# Calculate how long it will take to save until the down payment
current_savings = 0
annual_return = 0.04
months = 0

while current_savings < down_payment:
    salary_saved = portion_saved * (annual_salary / 12)
    return_on_investment = current_savings * (annual_return / 12)
    current_savings = current_savings + return_on_investment + salary_saved
    months += 1

print("Number of months:", months)
